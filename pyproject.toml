[project]
name = "lib-common"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "huncidd", email = "38997460@qq.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.3.3",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.12.0",
    "pyyaml>=6.0.3",
]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
